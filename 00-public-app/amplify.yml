version: 1

applications:
  - appRoot: 00-public-app
    frontend:
      phases:
        build:
          commands:
            - yarn run build
      artifacts:
        baseDirectory: ./public
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - .cache/**/*
          - public/static/**/*
